# MochaHost "Run NPM Install" must not fork Prisma scripts.
# prisma preinstall and @prisma/client postinstall hit CloudLinux nproc
# (fork: Resource temporarily unavailable / npm error 254).
ignore-scripts=true
